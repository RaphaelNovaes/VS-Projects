#include "Engine.h"
#include <iostream>

int main()
{
    // Declare an instance of Engine
    Engine engine;
    // Start the engine VRRrrrrmm
    engine.run();
    // Quit in the usual way when the engine is stopped
    return 0;

}
